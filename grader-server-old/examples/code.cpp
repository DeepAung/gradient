//go:build ignore
#include <bits/stdc++.h>

#define f first
#define s second
#define ll long long
#define pii pair<int, int>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);

    int n, a, b;
    cin >> n;
    while (n--) {
	cin >> a >> b;
	cout << a + b << "\n";
    }
    cout << "\n";

    return 0;
}
